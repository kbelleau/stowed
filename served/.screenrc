# beelleau - .screenrc - served
# configured on screen version 4.09.00

# environment defaults
env LANG=en_US.UTF-8

# default terminal type
term xterm-256color

# have screen inherit terminal emulator's cursor behavior
termcapinfo xterm* ti@:te@

# enable non-blocking mode to help when ssh connections are flaky
defnonblock 5

# as a fallback; set a scrollback size
defscrollback 10000

# default window size
hardstatus on
hardstatus alwayslastline
hardstatus string " %-w%{R}%n %t%{-}%+w%= %{R}%H "