;; -*- coding: utf-8; lexical-binding: t; -*-
;; beelleau selected-packages-config.el

(setq package-selected-packages
      '(modus-themes
        org-superstar
        indent-bars
        corfu
        vertico
        orderless
        lin
        substitute
        yasnippet
        eglot
        tramp
        paredit
        literate-scratch
        undo-fu
        inf-ruby
        yaml-mode
        sed-mode
        dockerfile-mode
        markdown-mode))
