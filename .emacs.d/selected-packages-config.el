;; -*- coding: utf-8; lexical-binding: t; -*-
;; kbelleau selected-packages-config.el

(setq package-selected-packages
      '(modus-themes
        org-superstar
        vertico
        orderless
        corfu
        lin
        substitute
        yasnippet
        tramp
        paredit
        eglot
        undo-fu
        go-mode
        yaml-mode
        json-mode
        sed-mode
        markdown-mode))
