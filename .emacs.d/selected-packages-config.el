;; -*- coding: utf-8; lexical-binding: t; -*-
;; kbelleau selected-packages-config.el

(setq package-selected-packages
      '(modus-themes
        org-superstar
        company
        indent-bars
        vertico
        orderless
        lin
        substitute
        yasnippet
        tramp
        paredit
        undo-fu
        inf-ruby
        yaml-mode
        sed-mode
        markdown-mode))
